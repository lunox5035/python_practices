클래스 : 설계도
class 클래스 이름:
    def 메서드이름(self{객체 자신}):
        명령블럭


객체 : 제품
객체=클래스 이름()
객체.메서드
=~의 메서드

속성 : 클래스 안의 변수
class 이름:
    def __init__(self,name): #생성자
       self.name=name

메서드 : 클래스 안의 함수

생성자 : 	객체를 만들때 (가장먼저)실행되는 함수

인스턴스 : 메모리에 살아있는 객체